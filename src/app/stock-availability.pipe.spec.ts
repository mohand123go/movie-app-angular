import { StockAvailabilityPipe } from './stock-availability.pipe';

describe('StockAvailabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new StockAvailabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
